///ShzFahad
#include<bits/stdc++.h>
using namespace std;
#define ll long long int
#define ld long double
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    ll t;
    cin >> t;
    while(t--)
    {
        ll n;
        cin >> n;
        map<string,ll>mp;
        for(ll i=0;i<n;i++)
        {
            string x;
            cin >> x;
            mp[x]++;
        }
        string s;
        cin >> s;
        ll tp=0;
        for(ll i=0;i<s.size();i++)
        {
            string temp;
            temp.push_back(s[i]);
            ll prev;
            if(mp[temp])
            {
                for(ll j=0;j<)
            }
        }
    }

}
